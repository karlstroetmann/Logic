formulas(sos).
all x (e * x = x).                              % e is a left neutral element
all x exists y (y * x = e).                     % existence of a left inverse
all x all y all z ((x * y) * z = x * (y * z)).  % associativity
end_of_list.

formulas(goals).
all x all y (x * y = y * x).                    % * is commutative
end_of_list.
