formulas(theory).

% Associativity

all x all y all z ((x * y) * z = x * (y * z)).

all x (x * e = x).
all x (i(x) * x = e).

% Is e also a left neutral element?

exists x (e * x != x).

end_of_list.
