program main;
    print( lcm( 18, 24 ) );

    procedure lcm(m, n);
        return min/ cm(m, n);
    end lcm;

    procedure cm(m, n);
        return multiple(m, n) * multiple(n, m);
    end cm;

    procedure multiple(m, n);
        return { m * i : i in { 1 .. n } };
    end multiple;
end main;
