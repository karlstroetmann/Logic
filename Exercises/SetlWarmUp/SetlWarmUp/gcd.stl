program main;
    print( gcd( 666, 36 ) );

    procedure gcd(m, n);
        return max/ cd(m, n);
    end gcd;

    procedure cd(m, n);
        return divisor(m) * divisor(n);
    end cd;

    procedure divisor(m);
        return { t in { 1 .. m } | m mod t = 0 };
    end divisor;
end main;
