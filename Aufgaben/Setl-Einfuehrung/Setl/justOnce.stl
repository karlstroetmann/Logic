program main;
    L := [1, 4,5,6,2,3];
    print("justOnce(", L, ") = ", justOnce(L));
    L := [1, 4,5,6,2,3, 7, 2];
    print("justOnce(", L, ") = ", justOnce(L));

    procedure justOnce(L);
        return #L = #{ x : x in L };
    end justOnce;
    
end main;