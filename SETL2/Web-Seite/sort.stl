program main;
    S := { 13, 5, 7, 2, 4 };
    print( "sort( ", S, " ) = ", sort(S) );
    print( 1 -/ S );
    -- turn a set into a sorted tuple

    procedure sort(S);
        return [ n in [1.. 0 max/ S] | n in S ];
    end sort;
end main;
