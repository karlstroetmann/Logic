program main;
    L := [ 13, 5, 7, 2, 4 ];
    printList(L);
    
    -- print a list one element per line
    procedure printList(L);
        if L = [] then
            return;
        end if;
        x fromb L;
        print(x);
        printList(L);
    end printList;
end main;
