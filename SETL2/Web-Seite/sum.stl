program main;
    print("Bitte Zahl eingeben");
    read(n);
    total := sum(n);
    print("Summe 0 + 1 + 2 + ... + ",n, " = ", total);

    procedure sum(n);
        if n = 0 then 
            return 0;
        else
            return n + sum(n-1);
        end if;
    end sum;
end main;
