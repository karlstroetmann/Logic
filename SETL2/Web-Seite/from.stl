program main;
    S := { 13, 5, 7, 2, 4 };
    printSet(S);
    
    -- print a set one element per line
    procedure printSet(S);
        if S = {} then
            return;
        end if;
        x from S;
        print(x);
        printSet(S);
    end printSet;
end main;
