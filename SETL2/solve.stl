program main;
    x := 0.0;
    loop
        old_x := x;
        x := cos(x);
        print(x);
        if abs(x - old_x) < 1.0e-13 then
            print("x = ", x);
            exit;
        end if;
    end loop;
end main;
