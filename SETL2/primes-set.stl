program main;
    n := 100;
    -- primes is the set of prime numbers that are less or equal than n.
    primes := { p in  {2..n} | { x in {1..p} | p mod x = 0 } = {1, p} };
    print( primes );
end main;
