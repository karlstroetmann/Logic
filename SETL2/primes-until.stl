program main;
    n := 97;
    primes := {};
    p := 2;
    until p = n loop
       if {t in primes | p mod t = 0} = {} then
          print(p);
          primes := primes + { p };
       end if;
       p := p + 1;
    end loop;
end main;
