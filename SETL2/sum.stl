program main;
    read(n);  -- does not work with windows
    total := sum(n);
    print("Sum 0 + 1 + 2 + ... + ", n, " = ", total);

    procedure sum(n);
        if n = 0 then 
            return 0;
        else
            return sum(n-1) + n;
        end if;
    end sum;
end main;
